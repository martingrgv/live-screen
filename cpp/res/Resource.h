#pragma once

// Resource IDs for Live Screen.
//
// Keep numeric IDs stable: external tools (Explorer, etc.) cache icons by
// (file, index). The first ICON in the .rc is what Windows uses as the
// executable's shell icon, so IDI_APP_ICON gets the lowest ID.

#define IDI_APP_ICON 101
